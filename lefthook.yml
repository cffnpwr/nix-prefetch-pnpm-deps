pre-commit:
  parallel: true
  jobs:
    - name: golangci-lint
      run: golangci-lint run ./...

    - name: go-mod-tidy
      run: go mod tidy
      stage_fixed: true

    - name: format
      glob: "*.{go,nix}"
      run: treefmt --fail-on-change {staged_files}
      stage_fixed: true
